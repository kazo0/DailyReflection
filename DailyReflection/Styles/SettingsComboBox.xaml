<?xml version="1.0" encoding="UTF-8" ?>
<!--
	DRSettingsComboBoxStyle — the Settings enum pickers (Sober Time Display, Theme)
	as real ComboBoxes that look like the page's FilledCardContentControl rows:
	surface card, 12px corners, 16,12 padding, Header as the row title (BodyMedium),
	the selected value beneath it (BodySmall, secondary), a chevron on the right, and
	the Material hover / pressed / focus overlays of the filled card. Tapping anywhere
	on the row opens the dropdown (Uno: forced below the row).

	Template parts Uno's ComboBox resolves by name are kept: Popup, PopupBorder,
	ScrollViewer and ContentPresenter. Uno writes the selection into ContentPresenter
	through ItemTemplate; it stays collapsed here because the row renders the value
	itself (ValueText) in the row's BodySmall look — a TextBlock produced by
	ItemTemplate would pick up Material's implicit BodyMedium TextBlock style instead.
	Both the value and the dropdown items go through SettingsChoiceConverter, and the
	items use DRSettingsComboBoxItemStyle (Material list rows) — both defined in this
	dictionary, since same-file StaticResource lookups are the reliable kind.

	No Uno.Material style keys are referenced by StaticResource; colors and
	typography come from ThemeResource palette / typography keys, which resolve
	lazily and follow the active theme (see PickerFlyouts.xaml for the same rule).
-->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:converters="using:DailyReflection.Converters"
					xmlns:uno="using:Uno.UI.Xaml.Controls">

	<converters:SettingsChoiceConverter x:Key="SettingsChoiceConverter" />

	<!--
		Dropdown items: Material list rows (48px, 16px inset, hover / pressed and
		selected state layers) — the default ComboBoxItem style would be Fluent's.
	-->
	<Style x:Key="DRSettingsComboBoxItemStyle"
		   TargetType="ComboBoxItem">
		<Setter Property="Background" Value="Transparent" />
		<Setter Property="Foreground" Value="{ThemeResource OnSurfaceBrush}" />
		<Setter Property="Padding" Value="16,0" />
		<Setter Property="MinHeight" Value="48" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="UseSystemFocusVisuals" Value="True" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ComboBoxItem">
					<Grid x:Name="RootGrid"
						  MinHeight="{TemplateBinding MinHeight}"
						  Background="{TemplateBinding Background}">
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="StateLayer.Background" Value="{ThemeResource OnSurfaceHoverBrush}" />
										<Setter Target="StateLayer.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="StateLayer.Background" Value="{ThemeResource OnSurfacePressedBrush}" />
										<Setter Target="StateLayer.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="RootGrid.Opacity" Value="0.38" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Selected">
									<VisualState.Setters>
										<Setter Target="StateLayer.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="SelectedUnfocused">
									<VisualState.Setters>
										<Setter Target="StateLayer.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="SelectedPointerOver">
									<VisualState.Setters>
										<Setter Target="StateLayer.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="SelectedPressed">
									<VisualState.Setters>
										<Setter Target="StateLayer.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="SelectedDisabled">
									<VisualState.Setters>
										<Setter Target="StateLayer.Opacity" Value="1" />
										<Setter Target="RootGrid.Opacity" Value="0.38" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<!-- Selected by default; the hover / pressed states swap the brush. -->
						<Border x:Name="StateLayer"
								Background="{ThemeResource PrimarySelectedBrush}"
								IsHitTestVisible="False"
								Opacity="0" />
						<ContentPresenter x:Name="ContentPresenter"
										  Padding="{TemplateBinding Padding}"
										  Content="{TemplateBinding Content}"
										  ContentTemplate="{TemplateBinding ContentTemplate}"
										  ContentTransitions="{TemplateBinding ContentTransitions}"
										  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
										  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

	<Style x:Key="DRSettingsComboBoxStyle"
		   TargetType="ComboBox">
		<Setter Property="ItemContainerStyle" Value="{StaticResource DRSettingsComboBoxItemStyle}" />
		<Setter Property="Background" Value="{ThemeResource SurfaceBrush}" />
		<Setter Property="Foreground" Value="{ThemeResource OnSurfaceBrush}" />
		<Setter Property="CornerRadius" Value="12" />
		<Setter Property="Padding" Value="16,12" />
		<Setter Property="HorizontalAlignment" Value="Stretch" />
		<Setter Property="HorizontalContentAlignment" Value="Stretch" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="TabNavigation" Value="Once" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
		<Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto" />
		<!-- Uno-only attached property (all heads here are Uno): open the list under the row, not over it. -->
		<Setter Property="uno:ComboBox.DropDownPreferredPlacement" Value="Below" />
		<Setter Property="ItemTemplate">
			<Setter.Value>
				<DataTemplate>
					<TextBlock Text="{Binding Converter={StaticResource SettingsChoiceConverter}}" />
				</DataTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="ComboBox">
					<Grid x:Name="LayoutRoot">
						<VisualStateManager.VisualStateGroups>
							<VisualStateGroup x:Name="CommonStates">
								<VisualState x:Name="Normal" />
								<VisualState x:Name="PointerOver">
									<VisualState.Setters>
										<Setter Target="HoverOverlay.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Pressed">
									<VisualState.Setters>
										<Setter Target="PressedOverlay.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="Disabled">
									<VisualState.Setters>
										<Setter Target="Card.Opacity" Value="0.38" />
									</VisualState.Setters>
								</VisualState>
							</VisualStateGroup>
							<VisualStateGroup x:Name="FocusStates">
								<VisualState x:Name="Focused">
									<VisualState.Setters>
										<Setter Target="FocusedOverlay.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="FocusedPressed">
									<VisualState.Setters>
										<Setter Target="FocusedOverlay.Opacity" Value="1" />
									</VisualState.Setters>
								</VisualState>
								<VisualState x:Name="PointerFocused" />
								<VisualState x:Name="FocusedDropDown" />
								<VisualState x:Name="Unfocused" />
							</VisualStateGroup>
							<VisualStateGroup x:Name="DropDownStates">
								<VisualState x:Name="Opened" />
								<VisualState x:Name="Closed" />
							</VisualStateGroup>
						</VisualStateManager.VisualStateGroups>

						<!-- The row: same shape as the FilledCardContentControl rows around it. -->
						<Grid x:Name="Card"
							  Background="{TemplateBinding Background}"
							  CornerRadius="{TemplateBinding CornerRadius}">
							<Grid Padding="{TemplateBinding Padding}">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*" />
									<ColumnDefinition Width="Auto" />
								</Grid.ColumnDefinitions>

								<StackPanel VerticalAlignment="Center">
									<!-- Row title (the ComboBox Header), BodyMedium like the other rows' titles. -->
									<TextBlock x:Name="HeaderText"
											   Text="{TemplateBinding Header}"
											   Foreground="{TemplateBinding Foreground}"
											   FontFamily="{ThemeResource BodyMediumFontFamily}"
											   FontWeight="{ThemeResource BodyMediumFontWeight}"
											   FontSize="{ThemeResource BodyMediumFontSize}"
											   CharacterSpacing="{ThemeResource BodyMediumCharacterSpacing}"
											   TextTrimming="CharacterEllipsis" />
									<!-- Current value, BodySmall / secondary like the other rows' detail text. -->
									<TextBlock x:Name="ValueText"
											   Text="{Binding SelectedItem, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource SettingsChoiceConverter}}"
											   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
											   FontFamily="{ThemeResource BodySmallFontFamily}"
											   FontWeight="{ThemeResource BodySmallFontWeight}"
											   FontSize="{ThemeResource BodySmallFontSize}"
											   CharacterSpacing="{ThemeResource BodySmallCharacterSpacing}"
											   TextTrimming="CharacterEllipsis" />
									<!-- Template part Uno's ComboBox writes the selection into; ValueText displays it. -->
									<ContentPresenter x:Name="ContentPresenter"
													  Visibility="Collapsed" />
								</StackPanel>

								<FontIcon Grid.Column="1"
										  Glyph="&#xE76C;"
										  FontSize="14"
										  VerticalAlignment="Center"
										  Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
							</Grid>

							<!-- Filled-card state overlays (Uno Toolkit MaterialFilledCardContentControlStyle). -->
							<Border x:Name="PressedOverlay"
									Background="{ThemeResource OnSurfacePressedBrush}"
									IsHitTestVisible="False"
									Opacity="0" />
							<Border x:Name="HoverOverlay"
									Background="{ThemeResource OnSurfaceHoverBrush}"
									IsHitTestVisible="False"
									Opacity="0" />
							<Border x:Name="FocusedOverlay"
									Background="{ThemeResource OnSurfaceFocusedBrush}"
									IsHitTestVisible="False"
									Opacity="0" />
						</Grid>

						<!-- Dropdown -->
						<Popup x:Name="Popup">
							<Border x:Name="PopupBorder"
									Margin="0,4,0,0"
									Background="{ThemeResource SurfaceBrush}"
									BorderBrush="{ThemeResource OnSurfaceLowBrush}"
									BorderThickness="1"
									CornerRadius="{TemplateBinding CornerRadius}">
								<ScrollViewer x:Name="ScrollViewer"
											  AutomationProperties.AccessibilityView="Raw"
											  CornerRadius="{TemplateBinding CornerRadius}"
											  HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
											  HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
											  VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
											  VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
											  ZoomMode="Disabled">
									<ItemsPresenter />
								</ScrollViewer>
							</Border>
						</Popup>
					</Grid>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>

</ResourceDictionary>
