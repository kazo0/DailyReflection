// MVUX bindable generation tool v3 (Uno.Extensions 7.2+ default): better Hot Reload
// support, and it names the generated view-models {Name}ViewModel instead of
// Bindable{Name}Model. Declared here rather than in GlobalUsings.cs — an assembly
// attribute above the global usings there is a compile error (CS1529), which is where
// the Uno tooling keeps trying to inject it.
[assembly: Uno.Extensions.Reactive.Config.BindableGenerationTool(3)]
